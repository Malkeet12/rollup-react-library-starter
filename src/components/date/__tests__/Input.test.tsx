import React from "react";
import { render } from "@testing-library/react";
import Input from "../Date";

describe("Input", () => {
  it("is truthy", () => {
    expect(Input).toBeTruthy();
  });
});
